<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rwgga.dongdong">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dongdong_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".main.LoginActivity"
            android:theme="@style/DD_LOGIN_Theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.JoinActivity"
            android:theme="@style/DD_LOGIN_Theme" />

        <activity
            android:name=".main.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".board.boardActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".club.clubActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".board.boardWriteActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".main.createClubActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".main.createClubKindActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".main_more.moreActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".main_more.moreOutActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".main_more.moreMyboardActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".club_more.moreClubActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".club_more.moreClubUsersActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".club.clubCalendarActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".club.clubCalendarPopup"
            android:theme="@android:style/Theme.Dialog"></activity>


        <activity
            android:name=".club_more.moreClubCalendarActivity"
            android:theme="@style/AppTheme"></activity>


        <activity
            android:name=".main.clubJoinActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".main_more.moreMywaitActivity"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".club_more.moreClubWaitActivity"
            android:theme="@style/AppTheme"></activity>

    </application>

</manifest>